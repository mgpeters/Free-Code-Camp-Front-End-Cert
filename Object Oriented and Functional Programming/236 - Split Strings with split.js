// You can use the split method to split a string into an array.
// split uses the argument you pass in as a delimiter to determine which points the string should be split at.

// The code below plits the string up by word (using spaces // " ") and adds them to the empty array


var string = "Split me into an array";
var array = [];

array = string.split(" "); // returns: ["Split", "me", "into", "an", "array"]
