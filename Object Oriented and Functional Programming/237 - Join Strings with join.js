// the join method to join each element of an array into a string separated by whatever delimiter you provide as an argument.

// The code below joins the array into a string by spaces (by using " " as the arguement delimiter)


var joinMe = ["Split","me","into","an","array"];
var joinedString = '';

joinedString = joinMe.join(" "); //returns "Split me into an array"
